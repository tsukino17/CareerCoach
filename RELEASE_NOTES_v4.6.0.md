# v4.6.0 (2026-03-31)

## 主要改动

- 行动力教练：/coach/action 从入口页升级为可直接对话页面（Enter 发送、Shift+Enter 换行），并完成整体视觉风格柔化（babyblue 空气感、边框更轻、灰蓝文字统一）。
- 行动力教练 API：新增 /api/coach/action/chat（先用轻量回复逻辑跑通链路，后续可替换为模型调用与上下文注入）。
- 身份愿景板：/coach/action/vision 新增「生成图片」分享图导出能力，并修复图片生成时的变量重名导致的编译问题。
- 用户中心：新增 /user-center 页面；/user 通过 middleware 重写到 /user-center（用户看到的链接仍为 /user），并将原「行动计划」入口替换为「行动力教练」入口，同时保留可编辑的愿景板页面入口。

## 清理与冗余

- 移除临时路由 /user2（对应页面文件已删除）。
- 右下角浮窗入口已不作为当前方案的一部分（采用 /user → /user-center 的统一入口与导航）。

